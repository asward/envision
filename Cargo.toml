[package]
name = "envision"
version = "0.1.0"
edition = "2024"
description = "See your environment clearly. Change it with precision."

[dependencies]
base64 = "0.22"
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
